# =============================================================================
# SPX Algo — environment variables template
# Copy to .env and fill in real values.  NEVER commit .env to git.
# =============================================================================

# ── Data APIs ─────────────────────────────────────────────────────────────────
FRED_API_KEY=your_fred_api_key_here          # https://fred.stlouisfed.org/docs/api/api_key.html

# ── IBKR Execution ────────────────────────────────────────────────────────────
IBKR_HOST=127.0.0.1
IBKR_PORT=7497                               # 7497 = TWS paper | 4002 = IB Gateway paper
IBKR_CLIENT_ID=1
EXECUTION_MODE=PAPER                         # PAPER | LIVE  — must be explicitly set to LIVE

# ── Position sizing override (optional) ──────────────────────────────────────
# N_CONTRACTS=1                              # default 1; must be a positive integer

# ── Scheduler timing (ET, 24-hour) ───────────────────────────────────────────
# SCHEDULE_TIME_ET=16:05
# DEADLINE_TIME_ET=16:30

# ── Alert channels (all optional — configure at least one for dead-man alerts)
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_ID/YOUR_TOKEN
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHAT_ID=your_telegram_chat_id

# ── Email / SMTP (for dead-man's switch and daily digest) ────────────────────
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=your_gmail_app_password            # use an App Password, NOT your account password
ALERT_EMAIL_TO=you@example.com

# ── Signal output directory override (optional) ──────────────────────────────
# SIGNAL_OUTPUT_DIR=output/signals
